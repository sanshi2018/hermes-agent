# Python 配置注释翻译

**名称 (name)**: mem0  
**版本 (version)**: 1.3.0  
**描述 (description)**:  
Mem0 — 基于服务端的大语言模型（LLM）事实提取系统，  
支持语义搜索、自动去重，  
以及可选的重排序功能（平台模式）。  

**pip 依赖项 (pip_dependencies)**:  
- `mem0ai>=2.0.10,<3`